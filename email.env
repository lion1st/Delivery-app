EMAIL_USER=cyubahiroallain1@gmail.com
EMAIL_PASS=1234
let orders = [];

function createOrder(userId, productId) {
  const order = {
    id: Date.now(),
    userId,
    productId,
    status: "processing"
  };

  orders.push(order);
  return order;
}